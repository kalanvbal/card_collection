/*import { ContextCartes } from "../context/ContextCartes";
import { useContext } from "react";

export const useContextCartes = () => {
  const context = useContext(ContextCartes);

  if (!context) {
    throw Error(
      "useWorkoutsContext must be used inside a WorkoutsContextProvider"
    );
  }

  return context;
};
*/
